apiVersion: v1
kind: PersistentVolume
metadata:
  name: csv-pv
spec:
  storageClassName: csv-path
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /app/csv_db